# 测试总览

## 测试原则

测试只验证本轮 PRD 中可交付、可观察、可验收的功能。产品定位争议、远程数据库是否接入等不属于本轮测试用例，应作为范围问题处理。

## 测试用例

| 用例 | 验证内容 | 验证方式 | 状态 |
| --- | --- | --- | --- |
| TC-001 | 上下文按字符预算裁剪并保留最新消息 | `pnpm --filter @ai-chat/server test` | ✅ |
| TC-002 | 配置默认值和环境变量解析正确 | `pnpm --filter @ai-chat/server test` | ✅ |
| TC-003 | 前端类型和构建通过 | `pnpm build` | ✅ |
| TC-004 | README/DEPLOY 与能力边界一致 | 人工检查 diff | ✅ |

## 测试完成标记

只有在命令执行并确认结果后，才能把状态改为 `✅`。

## 测试结论

本轮后端自动化测试和全量构建均已通过。README、DEPLOY 和版本文档已同步本地数据、安全边界和新增能力边界。
